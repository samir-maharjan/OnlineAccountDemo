@model OnlineAccountDemo.ViewModels.UserInfoVM

@{
    ViewData["Title"] = "Users";
    Layout = null;
}

<h1>Howdy! @Model.Name</h1>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">

<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    .main-div {
        margin: 100px auto;
        width: 650px;
        border-radius: 5px;
        box-shadow: rgba(100, 100, 111, 0.2) 0px 7px 29px 0px;
    }
</style>

<div class="container mt-5">
    <h2>Details Page</h2>
    <hr />

    <div class="row">
        <div class="col-md-6">
            <dl class="row">

                <dt class="col-sm-3">Address:</dt>
                <dd class="col-sm-9">@Model.Address</dd>

                <dt class="col-sm-3">Account Number:</dt>
                <dd class="col-sm-9">@Model.AccountNumber</dd>

                <dt class="col-sm-3">Total Balance:</dt>
                <dd class="col-sm-9">@Model.TotalBalance</dd>

                <dt class="col-sm-3">Status:</dt>

                @if (Model.Status)
                {
                    <dd class="col-sm-9">Active</dd>
                }
                else
                {
                    <dd class="col-sm-9">Not Active</dd>
                }

            </dl>
        </div>
    </div>

  

    @using (Html.BeginForm("accDeposited", "UserAccountDetails", FormMethod.Post, new { @class = "w-100" }))
    {
        <input class="w-50"type="number" name="amt" placeholder="Enter Amount You Want To Deposit">
        <button type="submit" class="btn btn-success w-100">Deposit</button>
    }

</div>




 @section Scripts {
<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>

<script>

    $(document).ready(function () {


        $("#_deposit").on("click", function () {
            $("#dep").toggle();
        });

        @* var deposit_amt = ("#depositAmt").val();
            $.ajax({
            type: 'POST', // or 'GET' depending on your server-side implementation
            dataType: 'json',
            data: { id: '@Model.orp01uin', has_published: checked },
            url: '@Url.Action("CheckHasPublication", "DeathCase")',
            success: function (item) {
            // Assuming item is an object with a property 'message'
            $.toaster(item.message);
            },
            error: function (xhr, status, error) {
            // Handle errors if necessary
            console.error(xhr.responseText);
            }
            });*@

        });
</script>
}




